console.log('Here');

/*
TODO: Exporting and Importing in ES6 Modules

console.log('Exporting Module');

const shippingCost = 10;
const cart = [];

// Exporting this function (Named Export)
export const addToChart = function (product, qunatity){
  cart.push({product, qunatity});
  console.log(`${qunatity} ${product} added to chart`);
}

const totalPrice = 237;
const totalQunatity = 34;

export {totalPrice, totalQunatity as tq};

// Exporting only one thing per module
export default function (product, quantity){
  cart.push({product, quantity});
  console.log(`${quantity} ${product} added to chart`);
}

 */

